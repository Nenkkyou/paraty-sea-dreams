import PlaceholderPage from "./PlaceholderPage";
import { Calendar } from "lucide-react";

const AdminReservas = () => {
  return (
    <PlaceholderPage
      title="Reservas"
      description="Gerencie todas as reservas confirmadas, pendentes e o calendário de passeios."
      icon={Calendar}
    />
  );
};

export default AdminReservas;
